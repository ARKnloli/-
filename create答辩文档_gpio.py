#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
GPIO输出模式补充内容（推挽输出、开漏输出等）
"""

from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH

def create_document():
    doc = Document()

    style = doc.styles['Normal']
    style.font.name = '微软雅黑'
    style.font.size = Pt(11)

    title = doc.add_heading('GPIO输出模式详解', 0)
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER

    doc.add_paragraph('')

    # =====================================================
    # 一、GPIO输出模式概述
    # =====================================================
    doc.add_heading('一、GPIO输出模式概述', level=1)

    doc.add_paragraph('''
STM32的GPIO有多种输出模式，每种模式适用于不同的应用场景。

【GPIO输出模式分类】
┌─────────────────┬─────────────────────────────────────────────┐
│ 输出模式        │ 说明                                        │
├─────────────────┼─────────────────────────────────────────────┤
│ 推挽输出        │ 可输出高电平和低电平，驱动能力强            │
│ 开漏输出        │ 只能输出低电平，需要外部上拉电阻            │
│ 复用推挽输出    │ 由外设（如UART、SPI）控制的推挽输出         │
│ 复用开漏输出    │ 由外设（如I2C）控制的开漏输出               │
└─────────────────┴─────────────────────────────────────────────┘
''')

    # =====================================================
    # 二、推挽输出（Push-Pull）
    # =====================================================
    doc.add_heading('二、推挽输出（Push-Pull）', level=1)

    doc.add_paragraph('''
【工作原理】
推挽输出由两个MOS管组成：
• 上管（PMOS）：输出高电平时导通
• 下管（NMOS）：输出低电平时导通

        VDD
         │
       [PMOS] ← 上管
         │
    ─────┼───── 输出
         │
       [NMOS] ← 下管
         │
        GND

【特点】
• 可以主动输出高电平和低电平
• 驱动能力强（可直接驱动LED、蜂鸣器等）
• 输出阻抗低
• 不需要外部上拉电阻

【输出电平】
• 输出高电平：PMOS导通，输出VDD（3.3V）
• 输出低电平：NMOS导通，输出GND（0V）

【应用场景】
┌─────────────────┬─────────────────────────────────────────────┐
│ 应用            │ 说明                                        │
├─────────────────┼─────────────────────────────────────────────┤
│ LED驱动         │ 直接驱动LED，高电平点亮或低电平点亮         │
│ 蜂鸣器驱动      │ 直接驱动有源蜂鸣器                          │
│ 电机控制        │ 控制L298N的IN1/IN2等引脚                    │
│ 继电器控制      │ 控制继电器的通断                            │
│ 电平转换        │ 输出特定电平信号                            │
└─────────────────┴─────────────────────────────────────────────┘

【本项目应用】
• 电机控制引脚（PA4/PA5/PA6/PA7等）：推挽输出
• 蜂鸣器引脚（PB0）：推挽输出
• LED指示灯：推挽输出
''')

    # =====================================================
    # 三、开漏输出（Open-Drain）
    # =====================================================
    doc.add_heading('三、开漏输出（Open-Drain）', level=1)

    doc.add_paragraph('''
【工作原理】
开漏输出只有一个MOS管：
• 下管（NMOS）：输出低电平时导通
• 上管不存在：输出高电平时为高阻态

        VDD（外部上拉）
         │
       [上拉电阻]
         │
    ─────┼───── 输出
         │
       [NMOS] ← 只有下管
         │
        GND

【特点】
• 只能主动输出低电平
• 输出高电平需要外部上拉电阻
• 可以实现"线与"功能（多个开漏输出并联）
• 可以实现电平转换（不同电压域通信）

【输出电平】
• 输出低电平：NMOS导通，输出GND（0V）
• 输出高电平：NMOS截止，输出由上拉电阻决定（通常为VDD）

【应用场景】
┌─────────────────┬─────────────────────────────────────────────┐
│ 应用            │ 说明                                        │
├─────────────────┼─────────────────────────────────────────────┤
│ I2C通信         │ SCL/SDA必须使用开漏输出                     │
│ 总线通信        │ 多个设备共享总线，实现"线与"                │
│ 电平转换        │ 不同电压域的设备通信                        │
│ 中断输出        │ 多个设备共享中断线                          │
└─────────────────┴─────────────────────────────────────────────┘

【为什么I2C要用开漏输出？】
1. 多设备支持：
   • 多个设备可以共享同一条总线
   • 任何设备都可以拉低总线
   • 避免总线冲突

2. 电平转换：
   • 不同电压的设备可以通信
   • 上拉电阻接不同的电压

3. 时钟同步：
   • 从设备可以拉低SCL进行时钟同步
   • 实现时钟拉伸（Clock Stretching）

【本项目应用】
• OLED的SCL（PB6）：复用开漏输出
• OLED的SDA（PB7）：复用开漏输出
''')

    # =====================================================
    # 四、复用推挽/开漏输出
    # =====================================================
    doc.add_heading('四、复用推挽/开漏输出', level=1)

    doc.add_paragraph('''
【复用输出模式】
当GPIO引脚由外设（如UART、SPI、I2C）控制时，需要配置为复用输出模式。

【复用推挽输出】
• 由外设控制输出电平
• 可以输出高电平和低电平
• 应用：UART TX、SPI MOSI、PWM输出等

【复用开漏输出】
• 由外设控制输出电平
• 只能输出低电平，高电平需要上拉
• 应用：I2C SCL/SDA

【本项目应用】
┌─────────────────┬─────────────────┬─────────────────────────────┐
│ 引脚            │ 模式            │ 说明                        │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ PA9 (UART TX)   │ 复用推挽输出    │ 蓝牙发送                    │
│ PA10 (UART RX)  │ 浮空输入        │ 蓝牙接收                    │
│ PB6 (I2C SCL)   │ 复用开漏输出    │ OLED时钟                    │
│ PB7 (I2C SDA)   │ 复用开漏输出    │ OLED数据                    │
│ PA0 (TIM2 CH1)  │ 复用推挽输出    │ 左侧电机PWM                 │
│ PA1 (TIM2 CH2)  │ 复用推挽输出    │ 右侧电机PWM                 │
│ PA4/PA5/PA6/PA7 │ 推挽输出        │ 电机方向控制                │
│ PB0             │ 推挽输出        │ 蜂鸣器控制                  │
└─────────────────┴─────────────────┴─────────────────────────────┘
''')

    # =====================================================
    # 五、输出模式选择指南
    # =====================================================
    doc.add_heading('五、输出模式选择指南', level=1)

    doc.add_paragraph('''
【选择原则】

1. 需要输出高电平和低电平？
   → 使用推挽输出

2. 需要多个设备共享总线？
   → 使用开漏输出

3. 需要外设控制（UART/SPI/I2C/PWM）？
   → 使用复用推挽或复用开漏输出

【常见场景选择表】
┌─────────────────┬─────────────────┬─────────────────────────────┐
│ 应用场景        │ 推荐模式        │ 原因                        │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ LED驱动         │ 推挽输出        │ 需要输出高/低电平           │
│ 蜂鸣器驱动      │ 推挽输出        │ 需要输出高/低电平           │
│ 电机控制        │ 推挽输出        │ 需要输出高/低电平           │
│ 继电器控制      │ 推挽输出        │ 需要输出高/低电平           │
│ UART TX         │ 复用推挽输出    │ 外设控制，需要高/低电平     │
│ UART RX         │ 浮空输入        │ 接收数据                    │
│ SPI MOSI        │ 复用推挽输出    │ 外设控制，需要高/低电平     │
│ SPI MISO        │ 浮空输入        │ 接收数据                    │
│ I2C SCL         │ 复用开漏输出    │ 多设备共享，需要上拉        │
│ I2C SDA         │ 复用开漏输出    │ 多设备共享，需要上拉        │
│ PWM输出         │ 复用推挽输出    │ 外设控制，需要高/低电平     │
│ 中断输入        │ 上拉/下拉输入   │ 检测电平变化                │
└─────────────────┴─────────────────┴─────────────────────────────┘

【本项目配置汇总】
┌─────────────────┬─────────────────┬─────────────────────────────┐
│ 引脚            │ 模式            │ 功能                        │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ PA0             │ 复用推挽输出    │ TIM2_CH1，左侧电机PWM       │
│ PA1             │ 复用推挽输出    │ TIM2_CH2，右侧电机PWM       │
│ PA4             │ 推挽输出        │ 左电机IN1                   │
│ PA5             │ 推挽输出        │ 左电机IN2                   │
│ PA6             │ 推挽输出        │ 右电机IN1                   │
│ PA7             │ 推挽输出        │ 右电机IN2                   │
│ PA9             │ 复用推挽输出    │ UART1_TX，蓝牙发送          │
│ PA10            │ 浮空输入        │ UART1_RX，蓝牙接收          │
│ PA15            │ 推挽输出        │ 右电机IN4（备用）           │
│ PB0             │ 推挽输出        │ 蜂鸣器                      │
│ PB1             │ 推挽输出        │ ENB1，左侧通道B使能         │
│ PB2             │ 推挽输出        │ ENB2，右侧通道B使能         │
│ PB6             │ 复用开漏输出    │ I2C1_SCL，OLED时钟          │
│ PB7             │ 复用开漏输出    │ I2C1_SDA，OLED数据          │
└─────────────────┴─────────────────┴─────────────────────────────┘
''')

    # =====================================================
    # 六、代码配置示例
    # =====================================================
    doc.add_heading('六、代码配置示例', level=1)

    doc.add_paragraph('''
【推挽输出配置】
// 电机控制引脚（PA4/PA5/PA6/PA7）
GPIO_InitStruct.Pin = GPIO_PIN_4 | GPIO_PIN_5 | GPIO_PIN_6 | GPIO_PIN_7;
GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;      // 推挽输出
GPIO_InitStruct.Pull = GPIO_NOPULL;               // 无上下拉
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;     // 高速
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

【复用推挽输出配置】
// UART TX（PA9）
GPIO_InitStruct.Pin = GPIO_PIN_9;
GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;           // 复用推挽输出
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

【复用开漏输出配置】
// I2C SCL/SDA（PB6/PB7）
GPIO_InitStruct.Pin = GPIO_PIN_6 | GPIO_PIN_7;
GPIO_InitStruct.Mode = GPIO_MODE_AF_OD;           // 复用开漏输出
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);

【浮空输入配置】
// UART RX（PA10）
GPIO_InitStruct.Pin = GPIO_PIN_10;
GPIO_InitStruct.Mode = GPIO_MODE_INPUT;           // 输入模式
GPIO_InitStruct.Pull = GPIO_NOPULL;               // 浮空
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);
''')

    # =====================================================
    # 七、常见问题
    # =====================================================
    doc.add_heading('七、常见问题', level=1)

    doc.add_paragraph('''
【问题1】为什么I2C要用开漏输出，不能用推挽输出？

回答：
1. 多设备冲突：
   • 如果用推挽输出，两个设备同时输出不同电平会短路
   • 开漏输出只能拉低，不会短路

2. 电平转换：
   • 不同电压的设备可以通信
   • 上拉电阻接不同的电压

3. 时钟同步：
   • 从设备可以拉低SCL进行时钟同步
   • 推挽输出无法实现

【问题2】为什么UART TX用推挽输出，不用开漏输出？

回答：
1. 单点通信：
   • UART是点对点通信，不需要多设备共享
   • 推挽输出驱动能力更强

2. 速度要求：
   • 推挽输出切换速度更快
   • 适合高速通信

3. 简单性：
   • 不需要外部上拉电阻
   • 电路更简单

【问题3】开漏输出不接上拉电阻会怎样？

回答：
• 输出低电平正常
• 输出高电平时为高阻态（不确定电平）
• 可能导致通信失败或误触发

【问题4】推挽输出和开漏输出的驱动能力有什么区别？

回答：
• 推挽输出：驱动能力强，可以直接驱动LED、蜂鸣器等
• 开漏输出：驱动能力弱，通常需要外部上拉电阻和驱动电路

【问题5】什么时候用上拉输入，什么时候用下拉输入？

回答：
• 上拉输入：默认高电平，检测低电平有效
• 下拉输入：默认低电平，检测高电平有效
• 浮空输入：电平不确定，需要外部电路确定电平
''')

    # 保存文档
    output_path = r'D:\stmf132\GPIO输出模式详解.docx'
    doc.save(output_path)
    print(f'文档已生成: {output_path}')

if __name__ == '__main__':
    create_document()
