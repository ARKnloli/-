# 蓝牙小车项目代表性程序代码

## 1. 主程序 (main.c) - 系统初始化与主循环

```c
/* USER CODE BEGIN Includes */
#include "motor_control.h"
#include "buzzer.h"
#include "bluetooth.h"
#include "oled.h"
#include "chinese_font.h"
#include <stdio.h>
#include <string.h>
/* USER CODE END Includes */

/* USER CODE BEGIN PV */
static uint16_t current_speed = 999;  /* 当前速度 (0-999)，初始全速 */
static uint8_t current_dir = 0;
static uint8_t display_update = 1;
/* USER CODE END PV */

int main(void)
{
  /* MCU Configuration */
  HAL_Init();
  SystemClock_Config();

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  MX_I2C1_Init();
  MX_TIM2_Init();
  MX_USART1_UART_Init();

  /* Initialize modules */
  Motor_Init();
  Buzzer_Init();
  BT_Init();
  OLED_Init();

  /* 初始化OLED显示 */
  OLED_Clear();
  OLED_ShowChinese(0, 0, CH_ZHI);      // 智
  OLED_ShowChinese(16, 0, CH_NENG);     // 能
  OLED_ShowChinese(32, 0, CH_XIAO);     // 小
  OLED_ShowChinese(48, 0, CH_CHE);      // 车

  /* 显示状态：停止 */
  OLED_ShowChinese(0, 2, CH_ZHUANG);    // 状
  OLED_ShowChinese(16, 2, CH_TAI);      // 态
  OLED_ShowString(32, 2, ": ", FONT_SIZE_8X16);
  OLED_ShowChinese(48, 2, CH_TING);     // 停
  OLED_ShowChinese(64, 2, CH_ZHI2);     // 止

  /* 显示速度 */
  OLED_ShowChinese(0, 4, CH_SU);        // 速
  OLED_ShowChinese(16, 4, CH_DU);       // 度
  OLED_ShowString(32, 4, ": 100%  ", FONT_SIZE_6X8);

  /* 显示接收计数 */
  OLED_ShowChinese(0, 6, CH_JIE);       // 接
  OLED_ShowChinese(16, 6, CH_SHOU);     // 收
  OLED_ShowString(32, 6, ": 0     ", FONT_SIZE_6X8);

  Buzzer_Success();
  BT_RegisterCallback(BT_CommandCallback);

  /* Infinite loop */
  while (1)
  {
    BT_Process();

    /* 更新OLED显示（中文） */
    if (display_update) {
        display_update = 0;

        /* 显示状态行 */
        OLED_ShowChinese(0, 2, CH_ZHUANG);
        OLED_ShowChinese(16, 2, CH_TAI);
        OLED_ShowString(32, 2, ": ", FONT_SIZE_8X16);

        /* 显示方向状态 */
        switch (current_dir) {
            case 0:  // 停止
                OLED_ShowChinese(48, 2, CH_TING);
                OLED_ShowChinese(64, 2, CH_ZHI2);
                OLED_ShowString(80, 2, "    ", FONT_SIZE_8X16);
                break;
            case 1:  // 前进
                OLED_ShowChinese(48, 2, CH_QIAN);
                OLED_ShowChinese(64, 2, CH_JIN);
                OLED_ShowString(80, 2, "    ", FONT_SIZE_8X16);
                break;
            case 2:  // 后退
                OLED_ShowChinese(48, 2, CH_HOU);
                OLED_ShowChinese(64, 2, CH_TUI);
                OLED_ShowString(80, 2, "    ", FONT_SIZE_8X16);
                break;
            case 3:  // 左转
                OLED_ShowChinese(48, 2, CH_ZUO);
                OLED_ShowChinese(64, 2, CH_ZHUAN);
                OLED_ShowString(80, 2, "    ", FONT_SIZE_8X16);
                break;
            case 4:  // 右转
                OLED_ShowChinese(48, 2, CH_YOU);
                OLED_ShowChinese(64, 2, CH_ZHUAN);
                OLED_ShowString(80, 2, "    ", FONT_SIZE_8X16);
                break;
        }

        /* 显示速度行 */
        OLED_ShowChinese(0, 4, CH_SU);
        OLED_ShowChinese(16, 4, CH_DU);
        OLED_ShowString(32, 4, ": ", FONT_SIZE_8X16);

        uint8_t speed_percent = (current_speed * 100) / MOTOR_PWM_MAX;
        char speed_str[8];
        sprintf(speed_str, "%3d%%", speed_percent);
        OLED_ShowString(48, 4, speed_str, FONT_SIZE_8X16);

        /* 显示接收行 */
        OLED_ShowChinese(0, 6, CH_JIE);
        OLED_ShowChinese(16, 6, CH_SHOU);
        OLED_ShowString(32, 6, ": ", FONT_SIZE_8X16);

        char rx_str[8];
        sprintf(rx_str, "%d   ", bt_rx_count);
        OLED_ShowString(48, 6, rx_str, FONT_SIZE_8X16);
    }
  }
}

/* BT command callback */
void BT_CommandCallback(uint8_t cmd, uint8_t param)
{
    switch (cmd) {
        case CMD_FORWARD:
            Car_Forward(current_speed);
            current_dir = 1;
            display_update = 1;
            break;
        case CMD_BACKWARD:
            Car_Backward(current_speed);
            current_dir = 2;
            display_update = 1;
            break;
        case CMD_TURN_LEFT:
            Car_TurnLeft(current_speed);
            current_dir = 3;
            display_update = 1;
            break;
        case CMD_TURN_RIGHT:
            Car_TurnRight(current_speed);
            current_dir = 4;
            display_update = 1;
            break;
        case CMD_STOP:
            Car_Stop();
            current_dir = 0;
            display_update = 1;
            Buzzer_Beep(100);
            break;
        case CMD_SPEED_UP:
            current_speed += 100;
            if (current_speed > MOTOR_PWM_MAX) current_speed = MOTOR_PWM_MAX;
            display_update = 1;
            break;
        case CMD_SPEED_DOWN:
            if (current_speed >= 100) current_speed -= 100;
            else current_speed = 0;
            display_update = 1;
            break;
        case CMD_BEEP:
            Buzzer_Beep(200);
            break;
        default:
            break;
    }
}
```

---

## 2. 蓝牙模块 (bluetooth.c) - 蓝牙通信

```c
/**
  * @brief  蓝牙模块实现
  * @note   JDY-31蓝牙模块，USART通信，9600波特率
  */

#include "bluetooth.h"
#include "usart.h"

/* 导出变量 */
volatile uint8_t bt_flag = 0;       /* 有新命令标志 */
volatile uint8_t bt_cmd = 0;        /* 收到的命令字符 */
volatile uint8_t bt_rx_count = 0;   /* 接收计数器 */
volatile uint8_t bt_debug = 0;      /* 调试：显示匹配结果 */

/* 私有变量 */
static uint8_t bt_rx_byte;          /* 单字节接收缓冲 */
static uint8_t bt_speed = 50;       /* 当前速度 (0-100) */
static BT_CallbackFunc bt_callback = NULL;

/* 初始化 */
void BT_Init(void)
{
    bt_flag = 0;
    bt_cmd = 0;
    bt_speed = 50;
    HAL_UART_Receive_IT(&huart1, &bt_rx_byte, 1);
}

/* 获取命令 */
uint8_t BT_GetCmd(void)
{
    uint8_t cmd = bt_cmd;
    bt_flag = 0;
    bt_cmd = 0;
    return cmd;
}

/* 清除命令标志 */
void BT_ClearCmd(void)
{
    bt_flag = 0;
    bt_cmd = 0;
}

/* 获取当前速度 */
uint8_t BT_GetSpeed(void)
{
    return bt_speed;
}

/* 获取最后收到的原始字节 */
uint8_t BT_GetLastByte(void)
{
    return bt_rx_byte;
}

/* 注册蓝牙命令回调函数 */
void BT_RegisterCallback(BT_CallbackFunc callback)
{
    bt_callback = callback;
}

/* 蓝牙处理函数，在主循环中调用 */
void BT_Process(void)
{
    if (bt_flag && bt_callback) {
        bt_callback(bt_cmd, 0);
        bt_flag = 0;
        bt_cmd = 0;
    }
}

/* 串口接收完成回调（中断中调用） */
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    if (huart->Instance == USART1) {
        uint8_t rx = bt_rx_byte;

        /* 增加接收计数器 */
        bt_rx_count++;

        /* 回显测试：收到什么就发回什么 */
        HAL_UART_Transmit(&huart1, &rx, 1, 100);

        /* 只设置flag，不做业务处理 */
        bt_debug = 0;

        if (rx == CMD_STOP || rx == CMD_FORWARD || rx == CMD_BACKWARD ||
            rx == CMD_TURN_LEFT || rx == CMD_TURN_RIGHT ||
            rx == CMD_SPEED_UP || rx == CMD_SPEED_DOWN ||
            rx == CMD_BEEP) {

            bt_cmd = rx;
            bt_flag = 1;
            bt_debug = 1;

            /* 加减速直接修改速度 */
            if (rx == CMD_SPEED_UP) {
                bt_speed += 10;
                if (bt_speed > 100) bt_speed = 100;
            } else if (rx == CMD_SPEED_DOWN) {
                if (bt_speed >= 10) bt_speed -= 10;
                else bt_speed = 0;
            }
        }

        /* 重新启动接收 */
        HAL_UART_Receive_IT(&huart1, &bt_rx_byte, 1);
    }
}
```

---

## 3. 电机控制 (motor_control.c) - 电机驱动

```c
/**
  * @brief  电机控制模块实现
  * @note   L298N电机驱动，双通道控制，支持PWM调速
  */

#include "motor_control.h"
#include "tim.h"

/* Private variables */
static uint16_t current_left_speed = 0;
static uint16_t current_right_speed = 0;

/**
  * @brief  电机控制模块初始化
  */
void Motor_Init(void)
{
    /* 启动PWM输出 */
    HAL_TIM_PWM_Start(&htim2, TIM_CHANNEL_1);  /* ENA (左电机) */
    HAL_TIM_PWM_Start(&htim2, TIM_CHANNEL_2);  /* ENB (右电机) */

    /* 设置初始速度为0 */
    __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, 0);
    __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_2, 0);

    /* ENB使能：PB1=HIGH, PB2=HIGH */
    HAL_GPIO_WritePin(ENB1_GPIO_Port, ENB1_Pin, GPIO_PIN_SET);
    HAL_GPIO_WritePin(ENB2_GPIO_Port, ENB2_Pin, GPIO_PIN_SET);

    /* 设置方向引脚为停止状态（通道A+通道B） */
    HAL_GPIO_WritePin(IN1_GPIO_Port, IN1_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN2_GPIO_Port, IN2_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN1B_GPIO_Port, IN1B_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN2B_GPIO_Port, IN2B_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN3_GPIO_Port, IN3_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN4_GPIO_Port, IN4_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN3B_GPIO_Port, IN3B_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN4B_GPIO_Port, IN4B_Pin, GPIO_PIN_RESET);

    current_left_speed = 0;
    current_right_speed = 0;
}

/**
  * @brief  设置单个电机速度和方向（同时控制通道A和通道B）
  */
void Motor_Set(Motor_IdTypeDef motor, Motor_DirTypeDef dir, uint16_t speed)
{
    /* 限制速度范围 */
    if (speed > MOTOR_PWM_MAX) {
        speed = MOTOR_PWM_MAX;
    }

    /* 设置方向 */
    switch (dir) {
        case MOTOR_DIR_FORWARD:
            if (motor == MOTOR_LEFT) {
                /* 左电机正转：通道A+通道B */
                HAL_GPIO_WritePin(IN1_GPIO_Port, IN1_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN2_GPIO_Port, IN2_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN1B_GPIO_Port, IN1B_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN2B_GPIO_Port, IN2B_Pin, GPIO_PIN_RESET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, speed);
            } else {
                /* 右电机正转：通道A+通道B */
                HAL_GPIO_WritePin(IN3_GPIO_Port, IN3_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN4_GPIO_Port, IN4_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN3B_GPIO_Port, IN3B_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN4B_GPIO_Port, IN4B_Pin, GPIO_PIN_RESET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_2, speed);
            }
            break;

        case MOTOR_DIR_BACKWARD:
            if (motor == MOTOR_LEFT) {
                /* 左电机反转：通道A+通道B */
                HAL_GPIO_WritePin(IN1_GPIO_Port, IN1_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN2_GPIO_Port, IN2_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN1B_GPIO_Port, IN1B_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN2B_GPIO_Port, IN2B_Pin, GPIO_PIN_SET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, speed);
            } else {
                /* 右电机反转：通道A+通道B */
                HAL_GPIO_WritePin(IN3_GPIO_Port, IN3_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN4_GPIO_Port, IN4_Pin, GPIO_PIN_SET);
                HAL_GPIO_WritePin(IN3B_GPIO_Port, IN3B_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN4B_GPIO_Port, IN4B_Pin, GPIO_PIN_SET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_2, speed);
            }
            break;

        case MOTOR_DIR_STOP:
        default:
            if (motor == MOTOR_LEFT) {
                /* 左电机停止：通道A+通道B */
                HAL_GPIO_WritePin(IN1_GPIO_Port, IN1_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN2_GPIO_Port, IN2_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN1B_GPIO_Port, IN1B_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN2B_GPIO_Port, IN2B_Pin, GPIO_PIN_RESET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, 0);
            } else {
                /* 右电机停止：通道A+通道B */
                HAL_GPIO_WritePin(IN3_GPIO_Port, IN3_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN4_GPIO_Port, IN4_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN3B_GPIO_Port, IN3B_Pin, GPIO_PIN_RESET);
                HAL_GPIO_WritePin(IN4B_GPIO_Port, IN4B_Pin, GPIO_PIN_RESET);
                __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_2, 0);
            }
            break;
    }
}

/**
  * @brief  停止所有电机
  */
void Motor_StopAll(void)
{
    /* 左电机停止 */
    HAL_GPIO_WritePin(IN1_GPIO_Port, IN1_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN2_GPIO_Port, IN2_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN1B_GPIO_Port, IN1B_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN2B_GPIO_Port, IN2B_Pin, GPIO_PIN_RESET);
    __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_1, 0);

    /* 右电机停止 */
    HAL_GPIO_WritePin(IN3_GPIO_Port, IN3_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN4_GPIO_Port, IN4_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN3B_GPIO_Port, IN3B_Pin, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(IN4B_GPIO_Port, IN4B_Pin, GPIO_PIN_RESET);
    __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_2, 0);
}

/**
  * @brief  小车前进
  */
void Car_Forward(uint16_t speed)
{
    Motor_Set(MOTOR_LEFT, MOTOR_DIR_FORWARD, speed);
    Motor_Set(MOTOR_RIGHT, MOTOR_DIR_FORWARD, speed);
}

/**
  * @brief  小车后退
  */
void Car_Backward(uint16_t speed)
{
    Motor_Set(MOTOR_LEFT, MOTOR_DIR_BACKWARD, speed);
    Motor_Set(MOTOR_RIGHT, MOTOR_DIR_BACKWARD, speed);
}

/**
  * @brief  小车左转
  */
void Car_TurnLeft(uint16_t speed)
{
    Motor_Set(MOTOR_LEFT, MOTOR_DIR_STOP, 0);
    Motor_Set(MOTOR_RIGHT, MOTOR_DIR_FORWARD, speed);
}

/**
  * @brief  小车右转
  */
void Car_TurnRight(uint16_t speed)
{
    Motor_Set(MOTOR_LEFT, MOTOR_DIR_FORWARD, speed);
    Motor_Set(MOTOR_RIGHT, MOTOR_DIR_STOP, 0);
}

/**
  * @brief  小车停止
  */
void Car_Stop(void)
{
    Motor_StopAll();
    current_left_speed = 0;
    current_right_speed = 0;
}
```

---

## 4. 蜂鸣器 (buzzer.c) - 蜂鸣器控制

```c
/**
  * @brief  蜂鸣器控制模块实现
  * @note   低电平触发蜂鸣器，PB0 → BUZZER
  */

#include "buzzer.h"

/* 蜂鸣器状态宏 */
#define BUZZER_ON()   HAL_GPIO_WritePin(BUZZER_PORT, BUZZER_PIN, GPIO_PIN_RESET)
#define BUZZER_OFF()  HAL_GPIO_WritePin(BUZZER_PORT, BUZZER_PIN, GPIO_PIN_SET)

/**
  * @brief  蜂鸣器初始化
  */
void Buzzer_Init(void)
{
    /* 初始状态为静音（高电平） */
    BUZZER_OFF();
}

/**
  * @brief  蜂鸣器开启（响）
  */
void Buzzer_On(void)
{
    BUZZER_ON();
}

/**
  * @brief  蜂鸣器关闭（静音）
  */
void Buzzer_Off(void)
{
    BUZZER_OFF();
}

/**
  * @brief  蜂鸣器翻转状态
  */
void Buzzer_Toggle(void)
{
    HAL_GPIO_TogglePin(BUZZER_PORT, BUZZER_PIN);
}

/**
  * @brief  蜂鸣器短响（用于提示音）
  * @param  ms: 响的时间（毫秒）
  */
void Buzzer_Beep(uint16_t ms)
{
    BUZZER_ON();
    HAL_Delay(ms);
    BUZZER_OFF();
}

/**
  * @brief  蜂鸣器响两声（成功提示）
  */
void Buzzer_Success(void)
{
    BUZZER_ON();
    HAL_Delay(100);
    BUZZER_OFF();
    HAL_Delay(100);
    BUZZER_ON();
    HAL_Delay(100);
    BUZZER_OFF();
}

/**
  * @brief  蜂鸣器响三声（错误提示）
  */
void Buzzer_Error(void)
{
    uint8_t i;
    for (i = 0; i < 3; i++) {
        BUZZER_ON();
        HAL_Delay(150);
        BUZZER_OFF();
        HAL_Delay(150);
    }
}
```

---

## 5. 蓝牙命令定义 (bluetooth.h) - 命令定义

```c
/**
  * @brief  蓝牙命令定义
  */

#ifndef __BLUETOOTH_H
#define __BLUETOOTH_H

#include "main.h"

/* 蓝牙命令定义 - 单字节ASCII字符 */
#define CMD_STOP        '0'    /* 停止 */
#define CMD_FORWARD     '1'    /* 前进 */
#define CMD_BACKWARD    '2'    /* 后退 */
#define CMD_TURN_LEFT   '3'    /* 左转 */
#define CMD_TURN_RIGHT  '4'    /* 右转 */
#define CMD_SPEED_UP    '5'    /* 加速 */
#define CMD_SPEED_DOWN  '6'    /* 减速 */
#define CMD_BEEP        '7'    /* 蜂鸣器 */

/* 蓝牙命令定义 - HEX格式 */
#define CMD_STOP_HEX        0x30
#define CMD_FORWARD_HEX     0x31
#define CMD_BACKWARD_HEX    0x32
#define CMD_TURN_LEFT_HEX   0x33
#define CMD_TURN_RIGHT_HEX  0x34
#define CMD_SPEED_UP_HEX    0x35
#define CMD_SPEED_DOWN_HEX  0x36
#define CMD_BEEP_HEX        0x37

/* 函数声明 */
void BT_Init(void);
uint8_t BT_GetCmd(void);
void BT_ClearCmd(void);
uint8_t BT_GetSpeed(void);
uint8_t BT_GetLastByte(void);
void BT_RegisterCallback(BT_CallbackFunc callback);
void BT_Process(void);

/* 回调函数类型定义 */
typedef void (*BT_CallbackFunc)(uint8_t cmd, uint8_t param);

#endif /* __BLUETOOTH_H */
```

---

## 6. 中断处理 (stm32f1xx_it.c) - USART中断

```c
/**
  * @brief  USART1全局中断处理
  */
extern UART_HandleTypeDef huart1;

void USART1_IRQHandler(void)
{
    /* 调用HAL库处理函数 */
    HAL_UART_IRQHandler(&huart1);
}
```

---

## 7. GPIO初始化 (gpio.c) - 引脚配置

```c
/**
  * @brief  GPIO初始化
  */
void MX_GPIO_Init(void)
{
    GPIO_InitTypeDef GPIO_InitStruct = {0};

    /* GPIO Ports Clock Enable */
    __HAL_RCC_GPIOD_CLK_ENABLE();
    __HAL_RCC_GPIOA_CLK_ENABLE();
    __HAL_RCC_GPIOB_CLK_ENABLE();

    /*Configure GPIO pin Output Level - 所有电机引脚初始化为低电平 */
    HAL_GPIO_WritePin(GPIOA, IN1_Pin|IN2_Pin|IN3_Pin|IN4_Pin
                            |IN1B_Pin|IN2B_Pin|IN3B_Pin|IN4B_Pin, GPIO_PIN_RESET);

    /*Configure GPIO pin Output Level */
    HAL_GPIO_WritePin(BUZZER_GPIO_Port, BUZZER_Pin, GPIO_PIN_SET);  // 高电平 = 静音

    /*Configure GPIO pin Output Level - ENB初始化为高电平 */
    HAL_GPIO_WritePin(ENB1_GPIO_Port, ENB1_Pin, GPIO_PIN_SET);
    HAL_GPIO_WritePin(ENB2_GPIO_Port, ENB2_Pin, GPIO_PIN_SET);

    /*Configure GPIO pins : 电机引脚 */
    GPIO_InitStruct.Pin = IN1_Pin|IN2_Pin|IN3_Pin|IN4_Pin
                         |IN1B_Pin|IN2B_Pin|IN3B_Pin|IN4B_Pin;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

    /*Configure GPIO pin : BUZZER_Pin */
    GPIO_InitStruct.Pin = BUZZER_Pin;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    HAL_GPIO_Init(BUZZER_GPIO_Port, &GPIO_InitStruct);

    /*Configure GPIO pins : ENB引脚 */
    GPIO_InitStruct.Pin = ENB1_Pin|ENB2_Pin;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
    HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);

    /* 禁用JTAG，释放PA15用作普通GPIO */
    __HAL_RCC_AFIO_CLK_ENABLE();
    __HAL_AFIO_REMAP_SWJ_NOJTAG();
}
```

---

## 8. 蓝牙协议定义 (bluetooth.h) - 通信协议

```c
/**
  * @brief  蓝牙通信协议
  *
  * 协议设计：
  * - 单字节ASCII字符
  * - 无帧头帧尾，无校验
  * - 每个字符对应一个功能
  *
  * 命令表：
  * '0' (0x30): 停止
  * '1' (0x31): 前进
  * '2' (0x32): 后退
  * '3' (0x33): 左转
  * '4' (0x34): 右转
  * '5' (0x35): 加速
  * '6' (0x36): 减速
  * '7' (0x37): 蜂鸣器
  */
```

---

## 代码说明

| 模块 | 文件 | 功能 |
|------|------|------|
| 主程序 | main.c | 系统初始化、主循环、命令回调 |
| 蓝牙通信 | bluetooth.c | USART中断接收、命令解析 |
| 电机控制 | motor_control.c | PWM调速、方向控制、双通道 |
| 蜂鸣器 | buzzer.c | 提示音控制 |
| 命令定义 | bluetooth.h | 蓝牙协议、命令宏定义 |
| GPIO配置 | gpio.c | 引脚初始化、模式配置 |

---

**这些代码是蓝牙小车项目的核心模块，可以直接用于答辩演示！** 💻
