#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
蓝牙小车项目答辩问题与详细解答（完整版）
包含GPIO输出模式详解
"""

from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH

def create_document():
    doc = Document()

    style = doc.styles['Normal']
    style.font.name = '微软雅黑'
    style.font.size = Pt(11)

    title = doc.add_heading('STM32蓝牙智能小车', 0)
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER

    subtitle = doc.add_paragraph('答辩问题与详细解答（完整版）')
    subtitle.alignment = WD_ALIGN_PARAGRAPH.CENTER
    subtitle.runs[0].font.size = Pt(16)

    doc.add_paragraph('')

    # =====================================================
    # 一、项目概述
    # =====================================================
    doc.add_heading('一、项目概述', level=1)

    doc.add_paragraph('''
【项目名称】STM32蓝牙智能小车

【主控芯片】STM32F103C8T6（72MHz，64KB Flash，20KB SRAM）

【电源方案】6节1.5V干电池串联 = 9V

【实现功能】
• 基本功能（30分）：前进、后退、左转、右转、停止
• 提示音功能（20分）：不同操作播放不同提示音
• 扩展功能（15分）：通过蓝牙调整速度
• 机动分数（5分）：OLED中文显示、调试功能

【开发环境】
• IDE：Keil MDK-ARM / STM32CubeMX
• 语言：C语言
• 调试：ST-Link + 串口助手
''')

    # =====================================================
    # 二、硬件系统设计
    # =====================================================
    doc.add_heading('二、硬件系统设计', level=1)

    # 2.1 GPIO输出模式详解
    doc.add_heading('2.1 GPIO输出模式详解（重要！）', level=2)
    doc.add_paragraph('''
【GPIO输出模式分类】
┌─────────────────┬─────────────────────────────────────────────┐
│ 输出模式        │ 说明                                        │
├─────────────────┼─────────────────────────────────────────────┤
│ 推挽输出        │ 可输出高电平和低电平，驱动能力强            │
│ 开漏输出        │ 只能输出低电平，需要外部上拉电阻            │
│ 复用推挽输出    │ 由外设（如UART、SPI）控制的推挽输出         │
│ 复用开漏输出    │ 由外设（如I2C）控制的开漏输出               │
└─────────────────┴─────────────────────────────────────────────┘

【推挽输出原理】
推挽输出由两个MOS管组成：
• 上管（PMOS）：输出高电平时导通
• 下管（NMOS）：输出低电平时导通

        VDD
         │
       [PMOS] ← 上管
         │
    ─────┼───── 输出
         │
       [NMOS] ← 下管
         │
        GND

特点：
• 可以主动输出高电平和低电平
• 驱动能力强（可直接驱动LED、蜂鸣器等）
• 输出阻抗低
• 不需要外部上拉电阻

【开漏输出原理】
开漏输出只有一个MOS管：
• 下管（NMOS）：输出低电平时导通
• 上管不存在：输出高电平时为高阻态

        VDD（外部上拉）
         │
       [上拉电阻]
         │
    ─────┼───── 输出
         │
       [NMOS] ← 只有下管
         │
        GND

特点：
• 只能主动输出低电平
• 输出高电平需要外部上拉电阻
• 可以实现"线与"功能（多个开漏输出并联）
• 可以实现电平转换（不同电压域通信）

【为什么I2C要用开漏输出？】
1. 多设备支持：
   • 多个设备可以共享同一条总线
   • 任何设备都可以拉低总线
   • 避免总线冲突

2. 电平转换：
   • 不同电压的设备可以通信
   • 上拉电阻接不同的电压

3. 时钟同步：
   • 从设备可以拉低SCL进行时钟同步
   • 实现时钟拉伸（Clock Stretching）

【本项目GPIO配置】
┌─────────────────┬─────────────────┬─────────────────────────────┐
│ 引脚            │ 模式            │ 功能                        │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ PA0             │ 复用推挽输出    │ TIM2_CH1，左侧电机PWM       │
│ PA1             │ 复用推挽输出    │ TIM2_CH2，右侧电机PWM       │
│ PA4             │ 推挽输出        │ 左电机IN1                   │
│ PA5             │ 推挽输出        │ 左电机IN2                   │
│ PA6             │ 推挽输出        │ 右电机IN1                   │
│ PA7             │ 推挽输出        │ 右电机IN2                   │
│ PA9             │ 复用推挽输出    │ UART1_TX，蓝牙发送          │
│ PA10            │ 浮空输入        │ UART1_RX，蓝牙接收          │
│ PA15            │ 推挽输出        │ 右电机IN4（备用）           │
│ PB0             │ 推挽输出        │ 蜂鸣器                      │
│ PB1             │ 推挽输出        │ ENB1，左侧通道B使能         │
│ PB2             │ 推挽输出        │ ENB2，右侧通道B使能         │
│ PB6             │ 复用开漏输出    │ I2C1_SCL，OLED时钟          │
│ PB7             │ 复用开漏输出    │ I2C1_SDA，OLED数据          │
└─────────────────┴─────────────────┴─────────────────────────────┘

【输出模式选择指南】
┌─────────────────┬─────────────────┬─────────────────────────────┐
│ 应用场景        │ 推荐模式        │ 原因                        │
├─────────────────┼─────────────────┼─────────────────────────────┤
│ LED驱动         │ 推挽输出        │ 需要输出高/低电平           │
│ 蜂鸣器驱动      │ 推挽输出        │ 需要输出高/低电平           │
│ 电机控制        │ 推挽输出        │ 需要输出高/低电平           │
│ UART TX         │ 复用推挽输出    │ 外设控制，需要高/低电平     │
│ UART RX         │ 浮空输入        │ 接收数据                    │
│ I2C SCL         │ 复用开漏输出    │ 多设备共享，需要上拉        │
│ I2C SDA         │ 复用开漏输出    │ 多设备共享，需要上拉        │
│ PWM输出         │ 复用推挽输出    │ 外设控制，需要高/低电平     │
└─────────────────┴─────────────────┴─────────────────────────────┘

【代码配置示例】
// 推挽输出（电机控制引脚）
GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;

// 复用推挽输出（UART TX、PWM）
GPIO_InitStruct.Mode = GPIO_MODE_AF_PP;

// 复用开漏输出（I2C）
GPIO_InitStruct.Mode = GPIO_MODE_AF_OD;

// 浮空输入（UART RX）
GPIO_InitStruct.Mode = GPIO_MODE_INPUT;
GPIO_InitStruct.Pull = GPIO_NOPULL;
''')

    # 2.2 电源系统
    doc.add_heading('2.2 电源系统设计', level=2)
    doc.add_paragraph('''
【电源方案】
• 电池：6节1.5V干电池串联 = 9V
• L298N供电：9V电池+ → L298N的12V输入端
• STM32供电：L298N的5V输出 → STM32的5V引脚
• 蓝牙/OLED供电：STM32的3.3V引脚

【共地设计】（非常重要！）
电池GND → L298N GND → STM32 GND → 蓝牙GND → OLED GND → 蜂鸣器GND
（所有模块必须共地，否则通信不稳定）

【滤波电容】
每个L298N模块并联100~470μF电解电容：
• 正极接电池+
• 负极接GND
• 作用：储能、滤波、稳定电压
• 注意：电解电容有极性，接反会爆炸
''')

    # 2.3 蓝牙模块
    doc.add_heading('2.3 蓝牙模块连接（USART通信）', level=2)
    doc.add_paragraph('''
【通信方式】USART（通用同步异步收发器），不是I2C！

【硬件连接】
┌─────────────┬─────────────┬─────────────────────────────────┐
│ JDY-31引脚  │ STM32引脚   │ 说明                            │
├─────────────┼─────────────┼─────────────────────────────────┤
│ VCC         │ 3.3V        │ ⚠️ 不能接5V！会烧毁模块！       │
│ GND         │ GND         │ 共地                            │
│ TXD         │ PA10 (RX)   │ 蓝牙发送 → STM32接收            │
│ RXD         │ PA9  (TX)   │ STM32发送 → 蓝牙接收            │
└─────────────┴─────────────┴─────────────────────────────────┘

【GPIO配置】
• PA9 (TX)：复用推挽输出
• PA10 (RX)：浮空输入

【为什么要交叉连接？】
• TX是发送端（Transmit），RX是接收端（Receive）
• A设备的TX要连到B设备的RX
• A设备的RX要连到B设备的TX
• 这样才能实现双向通信

【通信参数】
• 波特率：9600 bps
• 数据位：8位
• 停止位：1位
• 校验位：无
• 模式：透传模式（数据直接转发）
''')

    # 2.4 OLED显示
    doc.add_heading('2.4 OLED显示模块连接（I2C通信）', level=2)
    doc.add_paragraph('''
【通信方式】I2C（集成电路总线），不是USART！

【硬件连接】
┌─────────────┬─────────────┬─────────────────────────────────┐
│ OLED引脚    │ STM32引脚   │ 说明                            │
├─────────────┼─────────────┼─────────────────────────────────┤
│ VCC         │ 3.3V        │ 供电                            │
│ GND         │ GND         │ 共地                            │
│ SCL         │ PB6 (I2C1)  │ I2C时钟线                       │
│ SDA         │ PB7 (I2C1)  │ I2C数据线                       │
└─────────────┴─────────────┴─────────────────────────────────┘

【GPIO配置】
• PB6 (SCL)：复用开漏输出
• PB7 (SDA)：复用开漏输出

【为什么I2C要用开漏输出？】
1. 多设备支持：多个设备可以共享同一条总线
2. 电平转换：不同电压的设备可以通信
3. 时钟同步：从设备可以拉低SCL进行时钟同步

【I2C通信特点】
• 只需2根线（SCL时钟 + SDA数据）
• 支持多设备挂载（通过设备地址区分）
• 速度：400kHz（快速模式）
• OLED设备地址：0x3C（固定地址）

【中文显示实现】
• 使用16x16汉字点阵字库
• 取模软件：单片机-LCD-LED-OLED中文点阵生成软件
• 取模方式：阴码，逐行式，顺向
• 格式转换：行列式 → 页列式（适配OLED_ShowIcon函数）
''')

    # 2.5 L298N电机驱动
    doc.add_heading('2.5 L298N电机驱动模块连接', level=2)
    doc.add_paragraph('''
【L298N模块简介】
• 双H桥驱动，可同时控制2个电机
• 支持PWM调速
• 输入电压：5-35V
• 输出电流：2A（每通道）

【每个模块有2个通道】
• 通道A：ENA, IN1, IN2 → OUT1, OUT2
• 通道B：ENB, IN3, IN4 → OUT3, OUT4

【模块1（左侧电机）连接】
┌─────────────┬─────────────┬─────────────────────────────────┐
│ L298N引脚   │ STM32引脚   │ 说明                            │
├─────────────┼─────────────┼─────────────────────────────────┤
│ 12V         │ 电池+ (9V)  │ 电机电源                        │
│ GND         │ 电池-       │ 共地                            │
│ 5V          │ STM32 5V    │ 跳线帽在，输出5V给STM32         │
│ ENA         │ PA0         │ TIM2_CH1，PWM调速               │
│ ENB         │ 3.3V        │ 通道B使能（跳线帽或直接接）     │
│ IN1         │ PA4         │ 通道A方向控制                   │
│ IN2         │ PA5         │ 通道A方向控制                   │
│ OUT1        │ 左前电机+   │ 通道A输出                       │
│ OUT2        │ 左前电机-   │ 通道A输出                       │
│ OUT3        │ 左后电机+   │ 通道B输出                       │
│ OUT4        │ 左后电机-   │ 通道B输出                       │
└─────────────┴─────────────┴─────────────────────────────────┘

【模块2（右侧电机）连接】
┌─────────────┬─────────────┬─────────────────────────────────┐
│ L298N引脚   │ STM32引脚   │ 说明                            │
├─────────────┼─────────────┼─────────────────────────────────┤
│ 12V         │ 电池+ (9V)  │ 电机电源                        │
│ GND         │ 电池-       │ 共地                            │
│ ENA         │ PA1         │ TIM2_CH2，PWM调速               │
│ ENB         │ 3.3V        │ 通道B使能（跳线帽或直接接）     │
│ IN1         │ PA6         │ 通道A方向控制                   │
│ IN2         │ PA7         │ 通道A方向控制                   │
│ OUT1        │ 右前电机-   │ ⚠️ 交换接线                     │
│ OUT2        │ 右前电机+   │ ⚠️ 交换接线                     │
│ OUT3        │ 右后电机+   │ 通道B输出                       │
│ OUT4        │ 右后电机-   │ 通道B输出                       │
└─────────────┴─────────────┴─────────────────────────────────┘

【GPIO配置】
• PA0 (ENA)：复用推挽输出（TIM2_CH1 PWM）
• PA1 (ENA)：复用推挽输出（TIM2_CH2 PWM）
• PA4/PA5/PA6/PA7 (IN1/IN2)：推挽输出
• ENB：直接接3.3V（不需要GPIO控制）

【为什么右前电机要交换接线？】
• 左右电机的物理安装方向是镜像的
• 不交换的话，右前电机方向会相反
• 交换后，发送"前进"指令时4个轮子都向前转

【ENB使能说明】
• ENB需要接高电平才能使能通道B
• 可以用跳线帽连接到5V
• 也可以直接接3.3V
• 如果ENB悬空，通道B不工作，OUT3/OUT4没有输出
''')

    # 2.6 蜂鸣器
    doc.add_heading('2.6 蜂鸣器连接', level=2)
    doc.add_paragraph('''
【硬件连接】
┌─────────────┬─────────────┬─────────────────────────────────┐
│ 蜂鸣器引脚  │ STM32引脚   │ 说明                            │
├─────────────┼─────────────┼─────────────────────────────────┤
│ VCC         │ 3.3V或5V    │ 供电                            │
│ GND         │ GND         │ 共地                            │
│ I/O         │ PB0         │ 低电平触发                      │
└─────────────┴─────────────┴─────────────────────────────────┘

【GPIO配置】
• PB0：推挽输出

【控制方式】
• 低电平触发（PB0=0时响）
• 高电平静音（PB0=1时停）
• 有源蜂鸣器，通电即响

【提示音设计】
┌──────────────┬──────────────┬─────────────────────────────────┐
│ 操作         │ 提示音       │ 实现方式                        │
├──────────────┼──────────────┼─────────────────────────────────┤
│ 收到任意指令 │ 短响一声     │ 响100ms                         │
│ 停止指令     │ 长响一声     │ 响200ms                         │
│ 蜂鸣器指令'7'│ 连续响       │ 响500ms                         │
│ 系统启动     │ 响一声       │ 自检提示                        │
└──────────────┴──────────────┴─────────────────────────────────┘
''')

    # 2.7 完整引脚分配
    doc.add_heading('2.7 完整引脚分配表', level=2)

    pin_table = doc.add_table(rows=18, cols=4)
    pin_table.style = 'Table Grid'

    pin_data = [
        ('STM32引脚', '功能', 'GPIO模式', '连接模块'),
        ('PA0', 'TIM2_CH1', '复用推挽输出', 'L298N左侧ENA'),
        ('PA1', 'TIM2_CH2', '复用推挽输出', 'L298N右侧ENA'),
        ('PA4', 'GPIO', '推挽输出', 'L298N左侧IN1'),
        ('PA5', 'GPIO', '推挽输出', 'L298N左侧IN2'),
        ('PA6', 'GPIO', '推挽输出', 'L298N右侧IN1'),
        ('PA7', 'GPIO', '推挽输出', 'L298N右侧IN2'),
        ('PA9', 'USART1_TX', '复用推挽输出', '蓝牙RXD'),
        ('PA10', 'USART1_RX', '浮空输入', '蓝牙TXD'),
        ('PA15', 'GPIO', '推挽输出', 'L298N右侧IN4（备用）'),
        ('PB0', 'GPIO', '推挽输出', '蜂鸣器'),
        ('PB1', 'GPIO', '推挽输出', 'ENB1，左侧通道B使能'),
        ('PB2', 'GPIO', '推挽输出', 'ENB2，右侧通道B使能'),
        ('PB6', 'I2C1_SCL', '复用开漏输出', 'OLED SCL'),
        ('PB7', 'I2C1_SDA', '复用开漏输出', 'OLED SDA'),
        ('PA13', 'SWDIO', '-', 'ST-Link调试'),
        ('PA14', 'SWCLK', '-', 'ST-Link调试'),
    ]

    for i, (pin, func, mode, module) in enumerate(pin_data):
        pin_table.rows[i].cells[0].text = pin
        pin_table.rows[i].cells[1].text = func
        pin_table.rows[i].cells[2].text = mode
        pin_table.rows[i].cells[3].text = module

    doc.add_paragraph('')

    # =====================================================
    # 三、软件系统设计
    # =====================================================
    doc.add_heading('三、软件系统设计', level=1)

    # 3.1 中断优先级
    doc.add_heading('3.1 中断优先级配置', level=2)
    doc.add_paragraph('''
【中断优先级概念】
STM32的中断优先级分为两部分：
• 抢占优先级（Preemption Priority）：高优先级可打断低优先级
• 子优先级（Sub Priority）：同一抢占优先级下，先响应哪个

【本项目中断配置】
┌─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│ 中断源          │ 抢占优先级      │ 子优先级        │ 说明            │
├─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ SysTick_IRQn    │ 0（最高）       │ 0               │ 系统滴答定时器  │
│ USART1_IRQn     │ 1               │ 0               │ 蓝牙数据接收    │
└─────────────────┴─────────────────┴─────────────────┴─────────────────┘

【配置代码】
HAL_NVIC_SetPriority(USART1_IRQn, 1, 0);
HAL_NVIC_EnableIRQ(USART1_IRQn);

【为什么这样配置？】
1. SysTick（抢占优先级0）：负责HAL_Delay()等延时函数，必须最高优先级
2. USART1（抢占优先级1）：接收蓝牙数据，保证及时接收
''')

    # 3.2 蓝牙协议
    doc.add_heading('3.2 蓝牙通信协议设计', level=2)
    doc.add_paragraph('''
【协议设计】
采用极简的单字节ASCII字符协议：

┌──────────┬──────────┬──────────────┬─────────────────────────┐
│ 字符     │ HEX值    │ 功能         │ 小车动作                │
├──────────┼──────────┼──────────────┼─────────────────────────┤
│ '0'      │ 0x30     │ 停止         │ 4个电机全部停止         │
│ '1'      │ 0x31     │ 前进         │ 4个电机同时正转         │
│ '2'      │ 0x32     │ 后退         │ 4个电机同时反转         │
│ '3'      │ 0x33     │ 左转         │ 左侧停，右侧正转       │
│ '4'      │ 0x34     │ 右转         │ 左侧正转，右侧停       │
│ '5'      │ 0x35     │ 加速         │ 速度+10%               │
│ '6'      │ 0x36     │ 减速         │ 速度-10%               │
│ '7'      │ 0x37     │ 蜂鸣器       │ 响500ms                │
└──────────┴──────────┴──────────────┴─────────────────────────┘
''')

    # 3.3 电机控制
    doc.add_heading('3.3 电机控制实现', level=2)
    doc.add_paragraph('''
【控制原理 - H桥电路】

L298N内部是H桥电路，通过控制4个开关来改变电流方向：

        电源+
         │
    ┌────┴────┐
    │         │
   [S1]     [S3]
    │         │
    ├──电机───┤
    │         │
   [S2]     [S4]
    │         │
    └────┬────┘
         │
        GND

【方向控制逻辑】
┌──────┬──────┬────────────┐
│ IN1  │ IN2  │ 电机状态   │
├──────┼──────┼────────────┤
│  0   │  0   │ 停止       │
│  1   │  0   │ 正转       │
│  0   │  1   │ 反转       │
│  1   │  1   │ 制动       │
└──────┴──────┴────────────┘

【PWM调速】
• 使用TIM2的PWM功能
• 频率：1kHz
• 分辨率：0-999
• 默认速度：999（100%）
''')

    # =====================================================
    # 四、答辩问题与详细解答
    # =====================================================
    doc.add_heading('四、答辩问题与详细解答', level=1)

    # 问题1
    doc.add_heading('问题1：请介绍一下你的蓝牙小车系统的整体架构？', level=2)
    doc.add_paragraph('''
【回答】
本系统采用"手机APP + 蓝牙模块 + STM32主控 + 电机驱动"的四层架构：

1. 手机APP端：发送单字节ASCII字符指令
2. 蓝牙通信层：JDY-31蓝牙模块，USART通信，9600波特率
3. 主控层：STM32F103C8T6微控制器，负责指令解析和外设控制
4. 驱动层：2个L298N电机驱动模块，控制4个直流电机
5. 输出层：电机、OLED显示、蜂鸣器提示
''')

    # 问题2
    doc.add_heading('问题2：推挽输出和开漏输出有什么区别？在你的项目中是怎么用的？', level=2)
    doc.add_paragraph('''
【回答】

1. 推挽输出：
   • 可以主动输出高电平和低电平
   • 驱动能力强
   • 不需要外部上拉电阻
   • 应用：LED驱动、蜂鸣器驱动、电机控制、UART TX

2. 开漏输出：
   • 只能主动输出低电平
   • 输出高电平需要外部上拉电阻
   • 可以实现"线与"功能
   • 应用：I2C通信（SCL/SDA）

3. 本项目应用：
   • 电机控制引脚（PA4/PA5/PA6/PA7）：推挽输出
   • 蜂鸣器引脚（PB0）：推挽输出
   • UART TX（PA9）：复用推挽输出
   • I2C SCL/SDA（PB6/PB7）：复用开漏输出

4. 为什么I2C要用开漏输出？
   • 多设备支持：多个设备可以共享总线
   • 电平转换：不同电压的设备可以通信
   • 时钟同步：从设备可以拉低SCL进行时钟同步
''')

    # 问题3
    doc.add_heading('问题3：蓝牙模块和OLED分别用什么通信方式？有什么区别？', level=2)
    doc.add_paragraph('''
【回答】

1. 蓝牙模块：USART通信
   • 引脚：PA9(TX)、PA10(RX)
   • 特点：全双工、点对点
   • GPIO模式：TX=复用推挽输出，RX=浮空输入

2. OLED显示：I2C通信
   • 引脚：PB6(SCL)、PB7(SDA)
   • 特点：半双工、多设备支持
   • GPIO模式：复用开漏输出

3. 两者区别：
   • USART：2根线（TX+RX），全双工，点对点
   • I2C：2根线（SCL+SDA），半双工，多设备支持
''')

    # 问题4
    doc.add_heading('问题4：中断优先级是如何配置的？为什么这样配置？', level=2)
    doc.add_paragraph('''
【回答】

1. 中断优先级配置：
   • SysTick_IRQn：抢占优先级0（最高）
   • USART1_IRQn：抢占优先级1

2. 为什么这样配置？
   • SysTick负责延时函数，必须最高优先级
   • USART1接收蓝牙数据，保证及时接收
''')

    # 问题5
    doc.add_heading('问题5：蜂鸣器是如何实现的？有哪些提示音？', level=2)
    doc.add_paragraph('''
【回答】

1. 硬件连接：PB0，低电平触发，有源蜂鸣器
2. GPIO模式：推挽输出
3. 提示音类型：
   • 收到指令：响100ms
   • 停止指令：响200ms
   • 蜂鸣器指令：响500ms
   • 系统启动：响一声
''')

    # 问题6
    doc.add_heading('问题6：电机控制是如何实现的？L298N的双通道是什么？', level=2)
    doc.add_paragraph('''
【回答】

1. L298N双通道：
   • 通道A：ENA, IN1, IN2 → OUT1, OUT2
   • 通道B：ENB, IN3, IN4 → OUT3, OUT4
   • ENB需要接高电平才能使能通道B

2. 控制原理（H桥电路）：
   • 正转：IN1=1, IN2=0
   • 反转：IN1=0, IN2=1
   • 停止：IN1=0, IN2=0

3. PWM调速：
   • 使用TIM2的PWM功能
   • 频率：1kHz
   • 分辨率：0-999
''')

    # 问题7
    doc.add_heading('问题7：项目开发过程中遇到了哪些问题？如何解决的？', level=2)
    doc.add_paragraph('''
【回答】

1. 蓝牙通信不工作：TX/RX接线接反，交换后解决
2. 蜂鸣器不响：面包板接触不良，重新插拔解决
3. 蜂鸣器开机一直响：gpio.c初始化时PB0设为低电平，改为高电平
4. OLED中文乱码：字库数据格式不正确，使用取模软件生成正确字库
5. 电机只有前轮转：ENB没有接高电平，通道B不工作，接3.3V解决
''')

    # 问题8
    doc.add_heading('问题8：项目的创新点是什么？', level=2)
    doc.add_paragraph('''
【回答】

1. 协议设计创新：从5字节帧协议简化为单字节ASCII
2. 双通道控制创新：利用L298N的双通道特性
3. OLED中文显示：使用16x16汉字点阵字库
4. 模块化设计：清晰的代码结构，独立的驱动模块
5. GPIO模式选择：根据应用场景选择合适的输出模式
''')

    # =====================================================
    # 五、附录
    # =====================================================
    doc.add_heading('五、附录', level=1)

    doc.add_heading('A. 蓝牙协议速查表', level=2)
    protocol_table = doc.add_table(rows=9, cols=3)
    protocol_table.style = 'Table Grid'

    protocol_data = [
        ('字符', 'HEX', '功能'),
        ('0', '0x30', '停止'),
        ('1', '0x31', '前进'),
        ('2', '0x32', '后退'),
        ('3', '0x33', '左转'),
        ('4', '0x34', '右转'),
        ('5', '0x35', '加速'),
        ('6', '0x36', '减速'),
        ('7', '0x37', '蜂鸣器'),
    ]

    for i, (c, h, f) in enumerate(protocol_data):
        protocol_table.rows[i].cells[0].text = c
        protocol_table.rows[i].cells[1].text = h
        protocol_table.rows[i].cells[2].text = f

    doc.add_paragraph('')

    doc.add_heading('B. GPIO输出模式速查表', level=2)
    gpio_table = doc.add_table(rows=6, cols=3)
    gpio_table.style = 'Table Grid'

    gpio_data = [
        ('模式', '特点', '应用场景'),
        ('推挽输出', '可输出高/低电平，驱动能力强', 'LED、蜂鸣器、电机控制'),
        ('开漏输出', '只能输出低电平，需要上拉', 'I2C、总线通信'),
        ('复用推挽输出', '外设控制，可输出高/低电平', 'UART TX、PWM'),
        ('复用开漏输出', '外设控制，只能输出低电平', 'I2C SCL/SDA'),
        ('浮空输入', '接收外部信号', 'UART RX'),
    ]

    for i, (mode, feature, app) in enumerate(gpio_data):
        gpio_table.rows[i].cells[0].text = mode
        gpio_table.rows[i].cells[1].text = feature
        gpio_table.rows[i].cells[2].text = app

    # 保存文档
    output_path = r'D:\stmf132\蓝牙小车答辩问题与完整解答v2.docx'
    doc.save(output_path)
    print(f'文档已生成: {output_path}')

if __name__ == '__main__':
    create_document()
