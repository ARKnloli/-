#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
蓝牙小车项目答辩问题与解答文档生成器
"""

from docx import Document
from docx.shared import Pt, Cm, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.style import WD_STYLE_TYPE

def create_document():
    doc = Document()

    # 设置文档样式
    style = doc.styles['Normal']
    style.font.name = '微软雅黑'
    style.font.size = Pt(11)

    # 标题
    title = doc.add_heading('蓝牙智能小车项目', 0)
    title.alignment = WD_ALIGN_PARAGRAPH.CENTER

    subtitle = doc.add_paragraph('答辩问题与解答')
    subtitle.alignment = WD_ALIGN_PARAGRAPH.CENTER
    subtitle.runs[0].font.size = Pt(16)

    doc.add_paragraph('')  # 空行

    # 项目基本信息
    doc.add_heading('一、项目基本信息', level=1)

    info_table = doc.add_table(rows=7, cols=2)
    info_table.style = 'Table Grid'

    info_data = [
        ('项目名称', 'STM32蓝牙智能小车'),
        ('主控芯片', 'STM32F103C8T6'),
        ('开发环境', 'Keil MDK-ARM / STM32CubeMX'),
        ('通信方式', '蓝牙 (JDY-31模块)'),
        ('驱动模块', 'L298N电机驱动 x2'),
        ('功能模块', 'OLED显示、蜂鸣器提示、4电机驱动'),
        ('控制方式', '手机APP蓝牙遥控'),
    ]

    for i, (key, value) in enumerate(info_data):
        info_table.rows[i].cells[0].text = key
        info_table.rows[i].cells[1].text = value

    doc.add_paragraph('')

    # 二、核心问题与解答
    doc.add_heading('二、答辩常见问题与解答', level=1)

    # 问题分类
    questions = [
        {
            'category': '系统架构类',
            'items': [
                {
                    'q': '请介绍一下你的蓝牙小车系统的整体架构？',
                    'a': '''本系统采用分层架构设计：

1. 硬件层：STM32F103C8T6微控制器作为主控，通过GPIO、PWM、I2C、USART等外设连接各模块
2. 驱动层：包括电机驱动(L298N)、蓝牙通信(JDY-31)、OLED显示(SSD1306)、蜂鸣器等底层驱动
3. 协议层：采用单字节ASCII字符协议，简化通信流程，便于调试
4. 应用层：主循环轮询+中断处理，实现电机控制、显示更新、蜂鸣器控制等功能

系统工作流程：手机APP发送ASCII指令 → 蓝牙模块接收 → USART中断处理 → 主循环解析执行 → 电机/显示/蜂鸣器响应'''
                },
                {
                    'q': '为什么选择STM32F103C8T6作为主控芯片？',
                    'a': '''选择STM32F103C8T6的原因：

1. 性能适中：72MHz主频，满足小车控制需求
2. 资源丰富：64KB Flash、20KB SRAM，足够运行程序
3. 外设齐全：多路USART、I2C、SPI、TIM，满足多模块连接
4. 成本低廉：适合学生项目和原型开发
5. 生态完善：开发工具(Keil/CubeMX)、参考资料丰富
6. 封装小巧：LQFP48封装，适合小型化设计'''
                },
                {
                    'q': '系统的软件架构是怎样的？',
                    'a': '''软件架构采用"中断+主循环"模式：

1. 中断层：
   - USART1_IRQHandler：蓝牙数据接收，设置标志位
   - TIM2_IRQHandler：PWM生成，控制电机速度

2. 主循环层：
   - 轮询蓝牙接收标志
   - 解析命令并执行对应动作
   - 更新OLED显示
   - 控制蜂鸣器

3. 驱动模块：
   - motor_control.c：电机方向和速度控制
   - bluetooth.c/h：蓝牙协议处理
   - oled.c：I2C OLED显示驱动
   - buzzer.c：蜂鸣器控制

这种架构保证了实时响应，同时代码结构清晰易维护。'''
                }
            ]
        },
        {
            'category': '硬件设计类',
            'items': [
                {
                    'q': '请说明电机驱动模块的连接方案？',
                    'a': '''采用2个L298N模块驱动4个电机的方案：

模块1（左侧）：
- ENA ← PA0 (TIM2_CH1, PWM调速)
- IN1 ← PA4 (方向控制)
- IN2 ← PA5 (方向控制)
- OUT1/OUT2 → 左前电机
- OUT3/OUT4 → 左后电机

模块2（右侧）：
- ENA ← PA1 (TIM2_CH2, PWM调速)
- IN1 ← PA6 (方向控制)
- IN2 ← PA7 (方向控制)
- OUT1/OUT2 → 右前电机（注意交换接线）
- OUT3/OUT4 → 右后电机

设计要点：
- 同侧电机并联，同步转动
- 右前电机因镜像安装需交换接线
- 使用PWM实现调速功能'''
                },
                {
                    'q': '为什么右前电机需要交换接线？',
                    'a': '''这是因为左右电机的物理安装方向是镜像的：

1. 问题分析：
   - 左侧电机正转时轮子向前转
   - 右侧电机正转时轮子向后转（因为安装方向相反）
   - 这导致前进时右侧轮子反转

2. 解决方案：
   - 交换右前电机的接线：OUT1接负极，OUT2接正极
   - 这样当L298N输出正转信号时，右前电机实际向前转

3. 效果：
   - 发送'1'（前进）时，4个轮子都向前转
   - 发送'2'（后退）时，4个轮子都向后转
   - 左转/右转时，差速转向正确'''
                },
                {
                    'q': '蓝牙模块是如何连接的？为什么TX/RX要交叉连接？',
                    'a': '''蓝牙模块连接方案：

硬件连接：
- STM32 PA9(TX) → JDY-31 RXD
- STM32 PA10(RX) ← JDY-31 TXD
- VCC → 3.3V（注意不是5V）
- GND → 共地

交叉连接原因：
1. 串口通信原理：
   - TX是发送端，RX是接收端
   - A设备的TX要连接到B设备的RX
   - A设备的RX要连接到B设备的TX

2. 本项目中：
   - STM32的TX(PA9)发送数据 → 蓝牙的RXD接收
   - 蓝牙的TXD发送数据 → STM32的RX(PA10)接收

3. 调试经验：
   - 初期曾将TX/RX接反，导致通信失败
   - 通过OLED显示RX计数为0发现问题
   - 交换接线后通信正常'''
                },
                {
                    'q': '电源系统是如何设计的？',
                    'a': '''电源系统设计：

1. 电源方案：
   - 4节干电池串联 = 6V
   - 电池+ → L298N的12V输入端
   - 电池- → L298N的GND
   - L298N的5V输出 → STM32的5V引脚

2. 共地设计：
   - 电池GND → L298N GND → STM32 GND → 蓝牙GND → OLED GND
   - 所有模块必须共地，否则通信不稳定

3. 滤波电容：
   - 每个L298N模块并联100~470μF电解电容
   - 作用：储能、滤波、稳定电压
   - 接法：正极接电池+，负极接GND

4. 注意事项：
   - 电解电容有极性，接反会爆炸
   - 蓝牙模块必须接3.3V，不能接5V
   - 电池电压低于4.5V时需更换'''
                }
            ]
        },
        {
            'category': '通信协议类',
            'items': [
                {
                    'q': '蓝牙通信协议是如何设计的？',
                    'a': '''采用极简的单字节ASCII字符协议：

协议设计：
| 字符 | HEX | 功能 |
|------|-----|------|
| '0' | 0x30 | 停止 |
| '1' | 0x31 | 前进 |
| '2' | 0x32 | 后退 |
| '3' | 0x33 | 左转 |
| '4' | 0x34 | 右转 |
| '5' | 0x35 | 加速 |
| '6' | 0x36 | 减速 |
| '7' | 0x37 | 蜂鸣器 |

设计优点：
1. 单字节传输，响应快
2. ASCII字符可读，便于调试
3. 无帧头帧尾，简化处理逻辑
4. 每个字符独立功能，易于扩展

对比参考项目：从5字节帧协议简化为单字节，提高了可靠性。'''
                },
                {
                    'q': '蓝牙数据接收是如何实现的？',
                    'a': '''采用中断方式接收蓝牙数据：

1. 初始化配置：
   - USART1配置为9600波特率
   - 开启接收中断(USART_IT_RXNE)
   - 配置NVIC优先级

2. 中断处理函数：
   void USART1_IRQHandler(void)
   {
       if(USART_GetITStatus(USART1, USART_IT_RXNE) == SET)
       {
           USART_ClearITPendingBit(USART1, USART_IT_RXNE);
           uint8_t data = USART_ReceiveData(USART1);
           bluetooth_process(data);  // 处理接收到的数据
       }
   }

3. 数据处理：
   - 在中断中只做最少的事：读取数据、设置标志
   - 复杂处理放在主循环中
   - 这样避免中断阻塞，保证实时性

4. 调试方法：
   - OLED显示RX计数和原始字节
   - 蜂鸣器响表示收到数据
   - 串口助手辅助调试'''
                },
                {
                    'q': '如何保证蓝牙通信的可靠性？',
                    'a': '''采取了以下措施保证通信可靠性：

1. 硬件层面：
   - 确保TX/RX正确交叉连接
   - 使用3.3V供电（不是5V）
   - 共地连接，减少干扰
   - 缩短连线长度

2. 协议层面：
   - 单字节协议，无帧格式错误风险
   - ASCII字符可打印，便于调试
   - 每个字符独立验证

3. 软件层面：
   - 中断接收，实时响应
   - 超时处理：未收到数据时自动停止
   - 错误处理：无效字符忽略

4. 调试验证：
   - 先用串口助手测试蓝牙模块
   - OLED显示接收状态
   - 蜂鸣器确认收到数据
   - 逐步测试每个功能'''
                }
            ]
        },
        {
            'category': '电机控制类',
            'items': [
                {
                    'q': 'PWM调速是如何实现的？',
                    'a': '''使用TIM2的PWM功能实现调速：

1. PWM配置：
   - TIM2_CH1(PA0)：左侧电机速度
   - TIM2_CH2(PA1)：右侧电机速度
   - 频率：1kHz
   - 分辨率：0-999

2. 速度控制：
   - TIM_SetCompare1(TIM2, speed)：设置左侧速度
   - TIM_SetCompare2(TIM2, speed)：设置右侧速度
   - speed范围：0(停止)~999(全速)

3. 速度档位：
   - 默认速度：500 (50%)
   - 加速：speed += 100，最大999
   - 减速：speed -= 100，最小0

4. 调试：
   - OLED显示当前速度百分比
   - 实际测试不同速度下的小车表现'''
                },
                {
                    'q': '电机方向控制的原理是什么？',
                    'a': '''通过L298N的IN1/IN2引脚控制电机方向：

1. 控制逻辑：
   | IN1 | IN2 | 电机状态 |
   |-----|-----|----------|
   | 0   | 0   | 停止 |
   | 1   | 0   | 正转 |
   | 0   | 1   | 反转 |
   | 1   | 1   | 制动 |

2. 前进实现：
   - 左侧：IN1=1, IN2=0 (PA4=1, PA5=0)
   - 右侧：IN1=1, IN2=0 (PA6=1, PA7=0)

3. 左转实现：
   - 左侧：停止 (PA4=0, PA5=0)
   - 右侧：正转 (PA6=1, PA7=0)

4. 右转实现：
   - 左侧：正转 (PA4=1, PA5=0)
   - 右侧：停止 (PA6=0, PA7=0)

5. 后退实现：
   - 左侧：IN1=0, IN2=1 (PA4=0, PA5=1)
   - 右侧：IN1=0, IN2=1 (PA6=0, PA7=1)'''
                },
                {
                    'q': '遇到过哪些电机相关的问题？如何解决的？',
                    'a': '''遇到并解决了以下电机问题：

1. 问题1：右前电机方向相反
   - 现象：发送前进命令，右前轮向后转
   - 原因：左右电机安装方向镜像
   - 解决：交换右前电机接线（OUT1接负极，OUT2接正极）

2. 问题2：电机不转
   - 可能原因：
     a) L298N的12V供电未接
     b) ENA/ENB未连接PWM信号
     c) IN1/IN2电平设置错误
   - 排查方法：
     a) 检查电源连接
     b) 用万用表测量引脚电平
     c) 单独测试每个电机

3. 问题3：电机转速不一致
   - 原因：电机个体差异、电池电压不稳
   - 解决：
     a) 并联电容稳定电压
     b) 代码中分别调整左右速度
     c) 使用新电池

4. 调试经验：
   - 先单独测试每个电机
   - 再测试同侧两个电机
   - 最后测试4个电机联动'''
                }
            ]
        },
        {
            'category': '显示与交互类',
            'items': [
                {
                    'q': 'OLED显示是如何实现的？',
                    'a': '''使用I2C接口驱动0.96寸OLED显示屏：

1. 硬件连接：
   - SCL → PB6 (I2C1时钟)
   - SDA → PB7 (I2C1数据)
   - VCC → 3.3V
   - GND → 共地

2. 驱动实现：
   - I2C地址：0x3C
   - 分辨率：128x64像素
   - 字体：6x8像素

3. 显示内容：
   - 第0行：Smart Car（标题）
   - 第2行：方向状态（STOP/FWD/BACK/LEFT/RIGHT）
   - 第4行：速度百分比（SPD: 50%）
   - 第6行：RX计数和原始字节
   - 第7行：当前方向

4. 刷新策略：
   - 状态变化时更新显示
   - 使用局部刷新减少闪烁
   - 避免频繁写入影响性能'''
                },
                {
                    'q': '蜂鸣器的作用是什么？如何控制？',
                    'a': '''蜂鸣器用于状态提示和反馈：

1. 硬件连接：
   - I/O引脚 → PB0
   - 低电平触发（有源蜂鸣器）
   - VCC → 3.3V或5V

2. 控制方式：
   void Buzzer_Beep(void)
   {
       GPIO_ResetBits(GPIOB, GPIO_Pin_0);  // 低电平，响
       Delay_ms(100);
       GPIO_SetBits(GPIOB, GPIO_Pin_0);    // 高电平，停
   }

3. 应用场景：
   - 收到蓝牙指令时响一声（确认收到）
   - 系统启动时响一声（自检）
   - 错误状态时连续响（报警）

4. 调试经验：
   - 初期蜂鸣器不响，发现是面包板接触不良
   - 重新插拔后正常工作
   - 低电平触发，初始化时PB0输出高电平（静音）'''
                }
            ]
        },
        {
            'category': '调试与测试类',
            'items': [
                {
                    'q': '项目开发过程中遇到了哪些问题？如何解决的？',
                    'a': '''主要问题及解决方案：

1. 蓝牙通信不工作（2026-06-18）
   - 现象：APP发送数据，STM32无反应
   - 排查：
     a) 检查波特率：9600，正确
     b) 检查接线：发现TX/RX接反
     c) 测试：交换PA9和PA10后正常
   - 经验：串口通信必须交叉连接

2. 蜂鸣器不响（2026-06-18）
   - 现象：代码调用Buzzer_Beep()但不响
   - 排查：
     a) 检查代码：逻辑正确
     b) 检查硬件：发现面包板接触不良
   - 解决：重新插拔，确保接触良好

3. OLED字体异常（2026-06-18）
   - 现象：显示的文字方向混乱
   - 原因：字体方向设置错误
   - 解决：使用6x8字体，正确设置方向

4. 电机方向问题（2026-06-19）
   - 现象：右前电机方向相反
   - 原因：镜像安装
   - 解决：交换接线

调试方法总结：
- 最小化测试：只保留一个功能
- LED/OLED指示：可视化调试
- 蜂鸣器反馈：声音确认
- 串口助手：数据抓包分析'''
                },
                {
                    'q': '如何验证系统的各项功能？',
                    'a': '''功能验证方法：

1. 蓝牙通信测试：
   - 工具：手机蓝牙串口APP
   - 步骤：发送'0'-'7'，观察OLED显示和蜂鸣器
   - 验证：RX计数增加，显示正确字符

2. 电机控制测试：
   - 单电机测试：单独连接一个电机，发送'1'-'4'
   - 双电机测试：测试同侧两个电机同步
   - 四电机测试：测试前进、后退、左转、右转

3. PWM调速测试：
   - 发送'5'(加速)、'6'(减速)
   - 观察OLED显示的速度百分比
   - 听电机转速变化

4. OLED显示测试：
   - 检查各个状态显示是否正确
   - 测试快速切换时是否闪烁

5. 蜂鸣器测试：
   - 发送'7'测试蜂鸣器
   - 收到任意指令时应响一声

6. 综合测试：
   - 连续发送多个指令
   - 测试边界条件（速度最大/最小）
   - 长时间运行稳定性测试'''
                },
                {
                    'q': '如果要扩展功能，你会如何设计？',
                    'a': '''可以考虑以下扩展方向：

1. 功能扩展：
   - 超声波避障：添加HC-SR04模块
   - 循迹功能：添加红外传感器
   - 机械臂：添加舵机控制
   - 摄像头：添加图传模块

2. 通信扩展：
   - WiFi控制：使用ESP8266模块
   - 遥控器：添加2.4G无线模块
   - 语音控制：添加语音识别模块

3. 控制扩展：
   - PID调速：实现精确速度控制
   - 闭环控制：添加编码器反馈
   - 自动驾驶：路径规划算法

4. 界面扩展：
   - 手机APP：开发专用控制APP
   - 上位机：PC端控制软件
   - 数据可视化：实时显示传感器数据

5. 硬件扩展：
   - 更大电池：延长续航
   - 更强电机：提高速度/负载
   - 更多传感器：温度、湿度、光照等

设计原则：
- 模块化设计，便于功能增减
- 预留接口，方便扩展
- 代码分层，降低耦合度'''
                }
            ]
        },
        {
            'category': '项目总结类',
            'items': [
                {
                    'q': '这个项目你学到了什么？',
                    'a': '''通过这个项目，我学到了：

1. 硬件知识：
   - STM32微控制器的使用
   - 电机驱动原理（L298N）
   - 串口通信（USART）
   - I2C协议（OLED显示）
   - 电源系统设计

2. 软件技能：
   - 嵌入式C编程
   - 中断处理机制
   - PWM调速原理
   - 模块化代码设计
   - 调试技巧

3. 工程经验：
   - 硬件接线规范
   - 问题排查方法
   - 文档编写习惯
   - 版本控制（Git）

4. 团队协作：
   - 需求分析
   - 方案设计
   - 测试验证
   - 问题沟通

5. 解决问题的能力：
   - 遇到问题不慌张
   - 系统性排查
   - 善用工具和文档
   - 总结经验教训'''
                },
                {
                    'q': '项目有什么不足之处？如何改进？',
                    'a': '''项目不足与改进方向：

1. 不足之处：
   - 无避障功能：只能手动控制
   - 无闭环控制：速度不够精确
   - 电池续航短：4节干电池容量有限
   - 控制距离有限：蓝牙约10米
   - 无状态反馈：不知道小车实际速度

2. 改进方案：
   - 添加超声波传感器实现避障
   - 添加编码器实现PID闭环调速
   - 使用锂电池提高续航
   - 使用WiFi模块扩大控制距离
   - 添加更多传感器反馈状态

3. 代码改进：
   - 添加错误处理机制
   - 优化中断处理逻辑
   - 增加配置参数可调
   - 完善注释和文档

4. 硬件改进：
   - 优化PCB布局
   - 增加电源滤波
   - 使用更好的电机驱动
   - 添加急停按钮'''
                },
                {
                    'q': '项目的创新点是什么？',
                    'a': '''项目的创新点：

1. 协议设计创新：
   - 从5字节帧协议简化为单字节ASCII
   - 降低通信复杂度，提高可靠性
   - 便于调试和扩展

2. 接线方案创新：
   - 解决镜像电机方向问题
   - 通过交换接线实现统一控制
   - 简化软件逻辑

3. 调试方法创新：
   - OLED实时显示调试信息
   - 蜂鸣器声音反馈
   - 多层次测试方法

4. 模块化设计：
   - 清晰的代码结构
   - 独立的驱动模块
   - 易于维护和扩展

5. 文档规范：
   - 详细的接线说明
   - 完整的开发日志
   - 清晰的问题记录

6. 实用性：
   - 成本低廉（<100元）
   - 易于复制
   - 适合教学演示'''
                }
            ]
        }
    ]

    # 写入问题
    for section in questions:
        doc.add_heading(section['category'], level=2)

        for i, item in enumerate(section['items'], 1):
            # 问题
            q_para = doc.add_paragraph()
            q_run = q_para.add_run(f"Q{i}: {item['q']}")
            q_run.bold = True
            q_run.font.size = Pt(12)

            # 解答
            a_para = doc.add_paragraph()
            a_run = a_para.add_run(f"A{i}: ")
            a_run.bold = True
            a_para.add_run(item['a'])

            doc.add_paragraph('')  # 空行分隔

    # 三、附录
    doc.add_heading('三、附录', level=1)

    doc.add_heading('A. 蓝牙协议速查表', level=2)
    protocol_table = doc.add_table(rows=9, cols=3)
    protocol_table.style = 'Table Grid'

    protocol_data = [
        ('字符', 'HEX', '功能'),
        ('0', '0x30', '停止'),
        ('1', '0x31', '前进'),
        ('2', '0x32', '后退'),
        ('3', '0x33', '左转'),
        ('4', '0x34', '右转'),
        ('5', '0x35', '加速'),
        ('6', '0x36', '减速'),
        ('7', '0x37', '蜂鸣器'),
    ]

    for i, (c, h, f) in enumerate(protocol_data):
        protocol_table.rows[i].cells[0].text = c
        protocol_table.rows[i].cells[1].text = h
        protocol_table.rows[i].cells[2].text = f

    doc.add_paragraph('')

    doc.add_heading('B. 引脚分配速查表', level=2)
    pin_table = doc.add_table(rows=12, cols=3)
    pin_table.style = 'Table Grid'

    pin_data = [
        ('引脚', '功能', '连接模块'),
        ('PA0', 'TIM2_CH1', 'L298N左电机ENA'),
        ('PA1', 'TIM2_CH2', 'L298N右电机ENA'),
        ('PA4', 'GPIO', 'L298N左电机IN1'),
        ('PA5', 'GPIO', 'L298N左电机IN2'),
        ('PA6', 'GPIO', 'L298N右电机IN1'),
        ('PA7', 'GPIO', 'L298N右电机IN2'),
        ('PA9', 'USART1_TX', '蓝牙RXD'),
        ('PA10', 'USART1_RX', '蓝牙TXD'),
        ('PB0', 'GPIO', '蜂鸣器'),
        ('PB6', 'I2C1_SCL', 'OLED SCL'),
        ('PB7', 'I2C1_SDA', 'OLED SDA'),
    ]

    for i, (p, f, m) in enumerate(pin_data):
        pin_table.rows[i].cells[0].text = p
        pin_table.rows[i].cells[1].text = f
        pin_table.rows[i].cells[2].text = m

    # 保存文档
    output_path = r'D:\stmf132\蓝牙小车答辩问题与解答.docx'
    doc.save(output_path)
    print(f'文档已生成: {output_path}')

if __name__ == '__main__':
    create_document()
